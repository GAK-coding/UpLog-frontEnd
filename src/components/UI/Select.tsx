interface Props {
  width: string;
  items: {
    value: string;
    label: string;
  }[];
}
export default function CustomSelect(props: Props) {
  return (
    <div>
      <div>CustomSelect</div>
    </div>
  );
}
